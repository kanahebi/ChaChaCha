<%= form_with(model: system_admin_client, local: true) do |form| %>
  <% if system_admin_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(system_admin_client.errors.count, "error") %> prohibited this system_admin_client from being saved:</h2>

      <ul>
        <% system_admin_client.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
