<p id="notice"><%= notice %></p>

<h1>System Admin Clients</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @system_admin_clients.each do |system_admin_client| %>
      <tr>
        <td><%= link_to 'Show', system_admin_client %></td>
        <td><%= link_to 'Edit', edit_system_admin_client_path(system_admin_client) %></td>
        <td><%= link_to 'Destroy', system_admin_client, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New System Admin Client', new_system_admin_client_path %>
